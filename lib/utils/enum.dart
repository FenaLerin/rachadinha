
enum SnackStatus { alert, normal, error }

enum ScreenState { waiting, error, done, empty, guest }

enum TimeCouse { mornong, afternoon, night }

enum PerfilDeAcesso { clinica, hospital, farmacia }

enum ActionType { edit, add }
